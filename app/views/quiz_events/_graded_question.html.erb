<table class="table table-bordered">
  <tr>
    <td class="quiz_event_section">
      <%= @presenter.grade_header %></td>
  </tr>
</table>

  
<table class="table table-bordered graded-question">
  <tr>
    <td colspan="2"><%= markdown(@presenter.graded_question.content) %></td>
  </tr> 
  <% @presenter.graded_question.answers.each do |answer| %>
  <tr>
    <td width="10%"><%= @presenter.grade_icon_for(answer) %></td>
    <td><%= answer.content %></td>
  </tr> 
  <% end %>
</table>


<% if @presenter.has_remarks? %>
  <%= render 'remarks' %>
<% end %>




